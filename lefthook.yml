pre-commit:
  commands:
    lint_ts:
      glob: "*.{ts,tsx}"
      run: npx eslint {staged_files} --fix
    format_other:
      glob: "*.{json,md}"
      run: npx prettier {staged_files} --write 
pre-push:
  commands:
    test_ts: npx jest --bail


